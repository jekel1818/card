//IMPORTANT nécessite python27

//Étape 1 -- Copiez le ficher cv2.pyc dans votre répertoire C:\Python27\Lib\site-packages
		  -- Le ficher cv2.pyc se trouve dans le dossier opencv, deux versions sont disponibles
		  
//Étape 2 -- Exécuter dans le répertoire : python detector.py --input test12.jpg
		  -- Pour recréer la base de donnée : python detector.py --createDb
		  -- Les deux instructions peuvent être exécuter simultanément: python detector.py --input test11.jpg --createDb